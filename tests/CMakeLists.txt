cmake_minimum_required(VERSION 3.25)

project(cdpm_tests)

enable_testing()

# add_test(
#     NAME unit.***
#     COMMAND ${CMAKE_COMMAND}
#         -DCMAKE_SOURCE_DIR=${CMAKE_SOURCE_DIR}
#         -P ${CMAKE_CURRENT_SOURCE_DIR}/unit/***.cmake
# )

# TODO: make function to register tests by module + pass/fail cases
